[package]
name = "corevm-vmctl"
version.workspace = true
edition = "2021"

[dependencies]

[target.'cfg(target_os = "linux")'.dependencies]
libcorevm = { path = "../../libs/libcorevm", default-features = false, features = ["linux"] }
