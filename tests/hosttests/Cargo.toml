[package]
name = "libcorevm-hosttests"
version.workspace = true
edition = "2021"

[dependencies]
libcorevm = { path = "../../libs/libcorevm", default-features = false, features = ["host_test"] }

[profile.release]
lto = "fat"
codegen-units = 1
