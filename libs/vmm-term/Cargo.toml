[package]
name = "vmm-term"
version = "0.1.0"
edition = "2021"
description = "Command registry and execution engine for the CoreVM terminal"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
