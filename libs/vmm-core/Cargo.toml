[package]
name = "vmm-core"
version.workspace = true
edition = "2021"
description = "Shared data models for corevm (VmConfig, enums) — used by vmmanager and vmm-server"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4"] }
